{
  "scripts": {
    "build": "esbuild src/main.ts --bundle --outfile=build/out.js",
    "watch": "esbuild src/main.ts --bundle --outfile=build/out.js --watch"
  },
  "dependencies": {
    "esbuild": "0.16.7"
  }
}
